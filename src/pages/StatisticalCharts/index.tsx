import React from "react";
import { MainTemplate } from "../../templates/MainTemplate";
import { Charts } from "../../components/Charts";

export const StatisticalCharts: React.FC = () => {
  return (
    <MainTemplate>
      <Charts />
    </MainTemplate>
  );
};
